  - name: "{{name}}"
    type: fallback
    proxies:
{% for provider in providers %}{% if provider.tier == tier.index %}{% if not provider.meta_only or meta %}{% if not udp_only or provider.type == 'trojan-ws' %}
      - {{ provider.name }}
{% endif %}{% endif %}{% endif %}{% endfor %}
    url: 'https://www.google.com/generate_204'
    interval: 30
    lazy: false
